---
name: etf-weekly-share-report
description: 生成、复核或定时推送沪深ETF周度份额变化榜和A股风格手机长图。适用于用户提到ETF申赎榜、ETF份额增减、周度ETF热点、指定周校对或每周ETF图片报告；不用于仅分析ETF价格涨跌、收益率或持仓配置的请求。
---

# ETF周度份额变化榜

使用本Skill附带的 `scripts/etf_weekly_report.py` 读取沪深交易所公开ETF总份额数据，生成净申购、净赎回各前10名及手机长图。

## 执行方式

1. 用户给出日期时，将其作为目标统计周；否则取当前日期之前最近一个周五。
2. 在用户当前可写项目目录运行脚本。优先使用Codex工作区自带、已安装 `pandas`、`openpyxl` 和 `Pillow` 的Python：

   ```powershell
   python <skill目录>\scripts\etf_weekly_report.py [--as-of YYYY-MM-DD]
   ```

3. 从脚本输出的“图片已生成”取得绝对路径；用图片查看工具检查名称、数字、列对齐和底部说明没有被截断。
4. 最终优先直接显示PNG长图，图片下方只给出一句话观察。除非用户要求明细或校对，不重复粘贴完整Markdown表格。

## 不可改变的数据口径

- 本周变化＝最新交易日ETF总份额－上周同期总份额，单位为亿份。
- 年内份额变化＝最新总份额－上年最后交易日总份额；该列可能包含年内份额拆分影响，因此不要称为精确的“年内净申赎”。
- “申赎”是份额变化的便捷称呼，不是二级市场成交额，也不是按价格折算的资金净流入。
- 范围为沪深两市权益ETF，排除货币、债券、商品和REIT。
- 数据源必须是上海证券交易所和深圳证券交易所公开ETF份额数据。基金全称和拆分记录可用于辅助校验，但不得代替交易所份额数据。
- 统计周内发生份额拆分或折算的ETF必须从周度排名剔除；异常份额变化无法完成拆分核验时，停止发布并说明原因，不得猜测。
- 两市数据日期不一致、数据缺失、脚本失败或图片生成失败时，不发布旧图或编造结果。

## 图片规范

- 采用1080像素宽的竖版手机长图。
- 使用A股习惯配色：正向份额变化为红色，负向份额变化为绿色。
- ETF名称使用完整法定名称并保留“ETF”，必要时最多两行，不用含义模糊的过度简称。
- 必须展示代码、本周变化、年内份额变化、最新总份额、统计区间、单位、口径和数据源。

## 指定日期校对

用户提供参考图片或要求复核某一周时，使用 `--as-of` 生成同周数据，逐项比对20只ETF的代码、排名和三列数值。明确报告完全一致项、差异项和差异原因；份额拆分造成的机械变化应单独说明。

## 定时推送

仅在用户要求定时任务时，使用Codex自动化功能创建或更新现有任务。默认沿用用户明确给出的北京时间；任务应运行本Skill脚本并直接显示生成的绝对路径PNG图片。避免创建重复任务。
